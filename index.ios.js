/**
 * Index app file
 * https://github.com/facebook/react-native
 * @flow
 */

import {AppRegistry} from 'react-native';
import Root from './components/Root';

AppRegistry.registerComponent('TheGArdian', () => Root);
